name: CI - Vérifications

on:
  # Déclenche le workflow sur les pull requests
  pull_request:
    branches: [main, develop]
  
  # Déclenche également sur les push vers develop
  push:
    branches: [develop]

jobs:
  quality:
    name: Vérification de la qualité du code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Installation des dépendances
        run: npm ci

      - name: Vérification TypeScript
        run: npm run astro check
        continue-on-error: false

      - name: Build du projet
        run: npm run build
        env:
          NODE_ENV: production

      - name: Vérification de la taille du build
        run: |
          DIST_SIZE=$(du -sh dist | cut -f1)
          echo "📦 Taille du build: $DIST_SIZE"
          
      - name: Commentaire sur la PR (si applicable)
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const distSize = require('child_process')
              .execSync('du -sh dist')
              .toString()
              .split('\t')[0];
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `✅ Build réussi!\n\n📦 Taille du build: ${distSize}`
            });
